Run pytest from the parent (music/) directory, e.g.

  python -m pytest tests/

or

  python -m pytest --cov=lib tests/

to get the lines missing coverage

  python3 -m pytest --cov=lib --cov-report term-missing tests/
